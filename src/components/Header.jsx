import React from "react";

const Header = () => (
  <header style={{
    background: "#1e3a8a",
    color: "#fff",
    padding: "1.5rem 0",
    textAlign: "center",
    fontSize: "2.5rem",
    fontWeight: "bold",
    letterSpacing: "2px",
    boxShadow: "0 2px 8px rgba(0,0,0,0.08)"
  }}>
    Show View Hostels
  </header>
);

export default Header;
